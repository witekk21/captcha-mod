package com.example.captchamod;\n\nimport net.neoforged.api.distmarker.Dist;\nimport net.neoforged.bus.api.SubscribeEvent;\nimport net.neoforged.fml.common.Mod;\nimport net.neoforged.neoforge.client.event.InputEvent;\nimport net.minecraft.client.Minecraft;\nimport org.lwjgl.glfw.GLFW;\n\n@Mod.EventBusSubscriber(modid = \"captchamod\", bus = Mod.EventBusSubscriber.Bus.FORGE, value = Dist.CLIENT)\npublic class CaptchaKeyHandler {\n    \n    // F10 key\n    private static final int KEY_BIND = GLFW.GLFW_KEY_F10;\n    private static boolean lastKeyState = false;\n    \n    @SubscribeEvent\n    public static void onKeyInput(InputEvent.Key event) {\n        Minecraft mc = Minecraft.getInstance();\n        \n        if (mc.player == null) {\n            return;\n        }\n        \n        if (event.getKey() == KEY_BIND && event.getAction() == GLFW.GLFW_PRESS) {\n            mc.setScreen(new CaptchaGuiScreen());\n        }\n    }\n}\n",
<parameter name="path">src/main/java/com/example/captchamod/CaptchaKeyHandler.java